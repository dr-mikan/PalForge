# PalForge dev queue — runs on world.ready, no key and no console needed.
#
#   <name>            run as soon as the world is ready
#   <seconds> <name>  run that many seconds after the world is ready
#
# The names are the pf_* actions. THE FULL LIST, and it was wrong until 2026-08-02: this line
# named ten while the same file ran pf_native, pf_keys, pf_uiroute and pf_uiz as live lines a
# hundred lines further down. A queue file whose own legend omits what it is running is worse
# than one with no legend.
#
#   pf_tests    pf_spawn    pf_teach    pf_mesh     pf_native   pf_keys
#   pf_uidecl   pf_uiroute  pf_uiz
#   pf_reflect  pf_pal      pf_watch    pf_title    pf_uislot   pf_uievents
#   pf_hooks    pf_hooks_all            + one pf_hook_<id> per declared test hook (see below)
#
# ONE ACTIONS ENTRY IS DELIBERATELY NOT IN THAT LIST: `pf_hook`, the console spelling that takes
# an id (`pf_hook pal-skills-equip`). A line here is `[delay] name` and carries no argument
# (core/autorun.lua:62-67), so a bare `pf_hook` in this file would resolve, run with no id, and
# print the hook listing instead of a measurement. The generated per-hook names in the last row
# above are what this file uses, and they exist for exactly that reason.
#
# ⚠️ DO NOT MAINTAIN THAT LIST BY HAND EITHER. The authority is palforge.test's ACTIONS table,
# and test/init.lua PRINTS it sorted at every startup — grep UE4SS.log for "console commands:"
# and what comes back is what this file may contain, generated rather than remembered.
# core/autorun.lua looks each name up in that table and warns when there is no match; it reads
# a list of NAMES and never code, so nothing here can run anything a keypress could not.
#
# Lines starting with # are ignored. Delete a line to stop it running.
#
# THIS SESSION'S GOAL: settle skill.hit and skill.equip without catching a pal.
# pf_spawn puts a ChickenPal in front of you — hit it with a melee weapon.
# pf_teach gives the nearest pal a passive skill 20 s later, by which time the spawn has landed.
# skill.equip is CLOSED and skill.hit is measured silent from both sides, so pf_spawn/pf_teach
# have done their job. Commented out rather than deleted — put them back if you want a pal.
# pf_spawn
# 20 pf_teach

# ★ pf_keys — PALWORLD'S WHOLE LIVE KEYMAP, PRINTED. Read-only, one shot, NOTHING TO DO IN GAME.
# This is the one that turns every future key question into a lookup instead of a deploy-and-hope.
# It reads the key config the PLAYER edits (UPalOptionSubsystem.KeyConfigSettings) plus the
# shipped DefaultInput.ini defaults, and prints four blocks between #### BEGIN keymap and
# #### END keymap:
#   1. the sources and change-watches, with their state — so an empty result always says WHY
#   2. every key Palworld has an action on, with the action name and where the mapping came from
#   3. ★ THE LOOKUP TABLE: one row per name UE4SS can bind — free / game / palforge / refused /
#      unknown. Copy that block out; it is the answer to "can I have F6?" forever after.
# It needs a LOADED WORLD (the config lives on a world subsystem), touches nothing, and writes
# nothing to your settings or save. 8 s is just to let the subsystem settle after world.ready.

# NOW: the last UI slot question. Read-only, one shot, no in-game action needed. The 10 s delay
# lets the game's own UI widgets come up, because the probe cross-checks against them.

# THE MESH CHAIN, END TO END — nothing to do but LOOK. 15 s after the world is ready it
# resolves every catalogued /Game/... path, then hangs a shipped wooden chest (SM_ChestWood)
# in front of your character with a red tint declared, waits 30 s, and destroys it again.
#   a chest in front of you           the whole chain works: path -> LoadAsset -> SetStaticMesh
#                                     -> a component you can see. It has never been watched.
#   the chest is RED                  the dynamic material instance and the measured parameter
#                                     names work too.
#   a chest that is NOT red           mesh yes, material names no — the MATDESC lines it prints
#                                     are the names that asset really carries.
#   no chest, `attachTo -> true`      it is attached where you cannot see it; raise OFFSET.z in
#                                     test/init.lua's pf_mesh and run again.
#   no chest, `attachTo -> false`     the [mesh] line above it names the step that refused.
# The ASSET OK / ASSET MISS lines at the top are the other half: `ASSET OK ABP.PinkCat` is the
# first time an animation blueprint has ever been resolved from a path in this tree.
# It touches only a component of PalForge's own and destroys it again — your character's own
# mesh, materials and save are never written to.

# ★ THE UI RUN. Two actions, in this order, and between them they answer everything the last
# run left open. Nothing here can trap you: no input mode is written by PalForge any more, and
# every panel takes itself down on the clock.
#
# ---- pf_uiroute (25 s) — READ-ONLY + WATCHERS. What PALWORLD does with UI, input and Esc. ----
# It prints, from the running game: which CommonUI layer tags the in-game layout registers, what
# every ACTIVE Palworld screen declares in InputConfig / GameMouseCaptureMode (that is where the
# input mode lives on this build — UPalActivatableWidget, Pal.hpp:13369), and the live parameter
# lists of BP_AddWidget / RegisterActionBinding / AddHUD. Then it arms six counting hooks.
#   WHAT TO DO: open your INVENTORY and close it, then press ESC to open the pause menu and ESC
#   again to close it. Nothing else. Verdicts print themselves at 30 s, 60 s and 90 s.
#   THE LINE THAT MATTERS: `inputModeChanged fired=N`. N > 0 confirms that Palworld's CommonUI
#   action router owns the input mode and derives it from the activatable widget stack — which is
#   why PalForge no longer calls SetInputMode at all, and therefore why Esc should work again.
#   N == 0 while you opened and closed menus would falsify that, and is the most valuable
#   possible result. `actionBound` SAMPLES name the UI actions the game binds — look for UIEscape
#   / UICancel, which is the mechanism a mod must join to be IN Esc's path rather than beside it.
# ⚠️ It arms hooks and UE4SS cannot unregister one; they stay until you quit the game.

# ---- pf_uiz (110 s) — THREE PANELS. Esc, the mouse half, the frame colour, and the new route. ----
#   1. WAIT for the panels: a RED one on top and a MAGENTA one under it, top-left. A third,
#      TEAL, appears only if the game's own layer route works — its absence is a result too.
#   2. ⚠️ PRESS ESC TWICE, FIRST. The game's menu must open AND close, exactly as with no mod
#      loaded. Two earlier runs broke this and it is the whole point of the run.
#   3. WITH THE MENU OPEN, click the MAGENTA panel's button. Its label must count up — that is
#      the click router reaching our own widget while the game has given the mouse away.
#   4. CLOSE the menu. Press INS, then END: only the RED panel's counter may move.
#   5. PRESS THE MIDDLE MOUSE BUTTON: only the MAGENTA panel's counter may move. ⚠️ your pal will
#      also be given an attack order — the button is the game's DirectAttackOrder and PalForge
#      declares `overrideButtons = { "middle" }` to SHARE it; a UE4SS keybind never consumes.
#   6. At 45 s the red panel leaves; press INS/END again and the magenta one must take them.
#      At 90 s the teal one goes, at 100 s the magenta one, and a verdict block prints at 105 s.
# WHAT EACH OUTCOME MEANS is written out in the log by the run itself. The short version:
#   Esc opens AND closes            the diagnosis holds and the mod is out of the router's way
#   Esc still broken                the diagnosis is WRONG — pf_uiroute's counters say who owns it
#   rootClass=WBP_PalCommonWindow_C the magenta panel wears the game's own window chrome
#   rootClass=Border                it does not; a [ui] warning above names what was missing
#   the MAGENTA is visible          a declared colour lands on a Border INSIDE a Frame, which is
#                                   the only place it can — `Frame{ color = }` is now refused
#   the TEAL panel appeared         BP_AddWidget works: PalForge can put a screen up the way the
#                                   game does, activated and registered with the action router
# Nothing is written to your save and every panel comes off by itself or with a reload (F9).

# ---- THE LAST TWO UI ITEMS. Uncomment ONE of the two blocks below, not both. ----
#
# ui-menubutton-inner-slot — one shot, ~2 s of log, nothing to do in game afterwards.
# Reads WBP_Title_MenuButton_C's own designer tree from inside a save; the delay is so the
# in-game UI it cross-checks against (inventory / build menu widgets) is up.
#10 pf_uislot
#
# ui-update-event — arms four counting hooks, then WANTS TWO THINGS FROM YOU:
#   1. play for a minute, opening and closing the inventory / build menu / pause menu
#   2. quit to the TITLE SCREEN and load your save again
# The hooks stay armed across that (UE4SS has no unregister), so the second load's storm is
# counted in full and the verdict prints itself. Leave the mod running; do not restart.
#pf_uievents
# PARKED, 2026-08-02. These four ran on every world load and were the reason a first log was
# never clean: pf_native, pf_keys and pf_uiroute all print several hundred lines between five
# and fifteen seconds after world.ready, and pf_uiz puts panels on screen at 130 s. None of
# them writes to a save — they are parked for legibility, not safety. Uncomment the one you
# want; running them one at a time is the point of this file.
#5 pf_native
#8 pf_keys
#15 pf_uiroute
#130 pf_uiz

# ---- QUEUED 2026-08-02: the first real hook run. Six lines, none of them writes. ----
#
# Every hook below is read-only: no save is touched, nothing is spent, no character is
# written to. The five hooks that DO write (pal-skills-equip, item-satiety-write,
# skill-projectile-spawn, mesh-color-change, building-unlock) are not here and could not run
# from here anyway — each additionally needs env.debugHooks[id] = true, which the deployed
# palforge_dev.lua leaves commented out.
#
# The order is deliberate and the delays are for LEGIBILITY, not for correctness:
#
#   0 s   pf_hooks                        the gate listing — what would run, and what would not
#   5 s   game-build-live                 which call carries the build string. The startup line
#                                         currently warns MISMATCH because GetBuildVersion
#                                         answered "++UE5+Release-5.1-CL-0", which is Unreal's
#                                         version and not Palworld's. This picks the right one.
#  10 s   keymap-key-coverage             FKEY against UE4SS's live Key table, both directions.
#                                         Needs nothing on screen.
#  18 s   mesh-actor-identity             THE A-1 confirmation. Reads whether a per-actor lookup
#                                         hits on the second and third building sweep under the
#                                         old handle key and under the new GetFullName key. Stand
#                                         near a base with structures in it.
#  30 s   building-record-orphans         the F-7 quarantine round trip. The state directory was
#                                         emptied on 2026-08-02, so this run is the clean
#                                         baseline every later one is compared against.
#  45 s   pf_tests                        the 481-check API suite, in a world. Run it once more
#                                         at the TITLE SCREEN afterwards: 28 checks need a world
#                                         and 10 need there to be none, and no single press can
#                                         cover both.
#
# Comment these out again before the next run, or the next world load repeats all six.
# ---- RUN 1 IS DONE. What it settled, so nobody re-runs it for nothing: ----
#   keymap-key-coverage        4 pass / 0 fail. UE4SS Key = 165 names, keymap.FKEY = 165 rows,
#                              identical in BOTH directions. Owed work §3's bullet is measured.
#   mesh-actor-identity        A-1 CONFIRMED IN GAME on sweeps 2 and 3: the handle key MISSED for
#                              an actor that certainly still existed, GetFullName HIT,
#                              rawequal false, uo.same true.
#   building-record-orphans    saveId = w_1DF0E44B..., a per-save name, and NO persistence file
#                              exists — F-8 observed holding, with building = 0 registered.
#   game-build-live            all three KismetSystemLibrary calls answer Unreal's identity, not
#                              Palworld's patch. main.lua now reads PalGameInstance.DisplayVersion
#                              instead, which is the string on the title screen.
#
# ---- RUN 2 IS DONE. All three questions it was queued for came back answered: ----
#   game-build-live        3 pass / 0 fail. PalGameInstance.DisplayVersion and
#                          PalUtility.GetDisplayVersion BOTH answer "v1.0.2.101103", and both
#                          match env.gameBuild. Palworld's own patch version is readable from
#                          Lua after all — the negative that run 1 established was only about
#                          the three UKismetSystemLibrary calls, and env.gameBuildLive is a
#                          real check now instead of context for a log reader.
#   mesh-actor-identity    A-1 confirmed a SECOND time, on BP_BuildObject_ItemChest_C this
#                          time rather than the workbench — handle key MISS, GetFullName key
#                          HIT, on a different actor class in a different session.
#   pf_tests               444 / 0 / 37 at the title screen and 466 / 0 / 15 in a save, which
#                          is EXACTLY what the three-environment model predicted. The four
#                          failures of run 1 are gone and none came back.
#
# ---- RUN 3: THE STORE. The format-3 store has never opened a file in a real save. ----
#
# The first two lines are read-only and answer the two questions the store pass left open;
# the third is the suite, which now carries 553 checks rather than 481.
#
#   0 s   pf_hooks                 the gate listing. FOUR NEW HOOKS since run 2, three of them
#                                  behind their own env.debugHooks opt-in — this line says
#                                  which are open before anything tries to run.
#   5 s   store-base-load-cost     WHAT A REAL BASE COSTS TO LOAD. Every number in the design's
#                                  cost table was taken on WSL2/ext4 against synthetic records;
#                                  the largest set this tree has ever produced is 18. Expect
#                                  "zero bytes, zero milliseconds" in a stock session — F-8
#                                  leaves building = 0 registered — and that IS the result.
#  15 s   building-actor-streaming THE HIGHEST-PRIORITY MEASUREMENT OF THE PASS, and the one
#                                  thing you have to do is WALK AWAY FROM THE BASE. It decides
#                                  whether R-1 (a miss QUARANTINES a record instead of deleting
#                                  it) fixed a live data-loss bug or a latent one. It prints one
#                                  line a second for 180 s; keep walking or fast-travel.
#  25 s   pf_tests                 expect 521 / 0 / 32 headless-equivalent; in a save the world
#                                  gated checks come in on top.
#
# Comment these out again before the next run.
pf_hooks
5 pf_hook_store_base_load_cost
15 pf_hook_building_actor_streaming
25 pf_tests

# =====================================================================================
# ★ THE GAME-REQUIRED TEST HOOKS (test/hooks/). Twenty-four measurements that CANNOT be taken
# with the game switched off, each one a declared hook naming the plan/TODO.md item it
# closes. Every line below is commented out; uncomment the ONE you are running.
#
# THEY NEED env.debug = true. With it off nothing under test/hooks is even loaded and
# these names do not exist — core/autorun will say `no action named "pf_hook_..."`, which
# is the honest answer and not a defect. Set it in Scripts/palforge_dev.lua:
#     local env = require("palforge.env"); env.dev = true; env.debug = true
#
# `pf_hooks` first, always: it lists every hook, whether its gate is open, and the exact
# sentence that opens a shut one. It needs nothing but a loaded world.
#5 pf_hooks
#
# ---- read-only, nothing to do in game ------------------------------------------------
#
# game-build-live — the cheapest one here and the one to run FIRST in any session whose log
# you intend to paste anywhere. main.lua asks the running game what build it is and records
# the answer in env.gameBuildLive; that read has never once been run, so which of
# GetBuildVersion / GetEngineVersion / GetGameName carries "v1.0.2.101103" is unknown. This
# prints all three raw. A run of nils is a result: it means no build string is readable on
# this engine and the startup line's `unknown` is final rather than a wiring fault.
#5 pf_hook_game_build_live
#
# keymap-key-coverage — THE CHEAPEST HOOK HERE AND THE ONLY ONE THAT NEEDS NOTHING. It
# declares no `needs` at all, because UE4SS's `Key` table is a process global rather than a
# world subsystem, so it answers at the title screen and during a load. It crosses that
# table against core/keyboard/base/keymap.lua's M.FKEY — one row per name, both directions
# — and names every drift. This is the half test/cases/ui.lua:1506 has to skip headlessly
# (there is no Key table outside the game), i.e. the ONLY guard against a name UE4SS will
# happily bind that PalForge can say nothing about. Expected result today: two empty drift
# lists over 165 names. Run it with delay 0; it reads two Lua tables and touches nothing.
#pf_hook_keymap_key_coverage
#
# mesh-actor-identity — A-1, the most consequential finding in the audit. Three sweeps of
# the building actors, six seconds apart, asking a table keyed on the ACTOR HANDLE and a
# table keyed on GetFullName whether each still hits for a structure that certainly still
# exists. A handle MISS plus a name HIT is the confirmation the whole per-actor re-key
# rests on. Stand near some buildings.
#20 pf_hook_mesh_actor_identity
#
# item-datatable-row-read (+ item-recipe-of) — can Product_Count / WorkAmount /
# Material1_Id be indexed off the struct dt:FindRow('Arrow') returns, or must a recipe be
# assembled column-by-column the way icons are. Both routes run; whichever answers is the
# implementation. Open a crafting bench first if the table is not loaded.
#20 pf_hook_item_datatable_row_read
#
# audio-custom-file-loader — the five-block enumeration that decides whether ANY runtime
# file loader exists on this build. A run of nils CLOSES the item permanently, so the nils
# are printed rather than skipped.
#20 pf_hook_audio_custom_file_loader
#
# building-record-orphans — F-7's quarantine round trip, against a REAL save file for the
# first time (it has only ever been run headlessly, against the "world" fallback bucket).
# How many records loaded, how many had no registered owner, how many are in quarantine and
# for which packs, and — the assertion that matters — that a record belonging to a pack
# which is merely NOT LOADED TODAY was kept rather than destroyed. ⚠️ THE DELAY IS PART OF
# THE MEASUREMENT: the one orphan pass per world runs after ORPHAN_GRACE_SCANS = 60 scans
# (~30 s), so 35 s is the earliest a run says anything about THIS session. Run it twice and
# it prints the delta across the pass, which is the round trip itself.
#35 pf_hook_building_record_orphans
#
# store-base-load-cost — WHAT A REAL BASE COSTS THE STORE TO LOAD, and the cheapest of the
# four store hooks: it opens no file it did not already find and writes nothing anywhere. It
# prints the actors standing here, the registered building definitions, every pack file with
# its bytes, the json.decode of those exact bytes timed on THIS machine, and — by merging the
# real documents back into one — what the same records would have cost in the old format-2
# single-file shape. That last ratio is the "5.8x" the design claimed from a synthetic
# 500-record set; this is the first time it has been put next to a real base. ⚠️ In a stock
# session the honest answer is ZERO BYTES AND ZERO MILLISECONDS, because F-8 leaves building = 0
# registered and a pack that registers nothing has no file. That is the result, not a failed
# run. To measure a base that costs something, publish a definition first (see A-1's ⚠️).
#5 pf_hook_store_base_load_cost
#
# mesh-texture-import-live — A-5. ImportFileAsTexture2D is wired with the right declared
# signature and HAS NEVER ONCE BEEN CALLED. This calls it on a real 8x8 PNG (which it
# writes next to itself, so "the import failed" can never mean "there was no file"), prints
# what came back and its class chain, then calls it AGAIN for the same path and says
# whether the new positives-only cache answered instead of re-importing — that second half
# is the whole point, and it is what decides whether Pal{ mesh = { texture = ... } } still
# leaks a UTexture2D per spawned pal. No opt-in: it writes nothing to your save. ⚠️ It does
# allocate ONE UTexture2D that nothing in this process can destroy.
#20 pf_hook_mesh_texture_import_live
#
# ---- read-only, but they need you to DO something -----------------------------------
#
# building-actor-streaming — ⚠️ THE HIGHEST-PRIORITY MEASUREMENT OF THE STORE PASS, and the one
# thing it asks of you is to WALK. Palworld separates a persistent Model from a transient
# ConcreteModel (FPalMapObjectSaveData, Pal.hpp:4639) and carries OnAvailable /
# OnNotAvailableConcreteModel delegate pairs on about ten classes — every sign of proximity
# streaming, and NOT ONE OF THEM HAS BEEN MEASURED. If base actors really do leave memory, then
# before R-1 PalForge DELETED every structure's pack state about three seconds after the player
# walked away from the base. Stand in the middle of a base, run this, then walk off in a
# straight line or fast-travel and keep going for two minutes. One line a second: how many
# PalBuildObject actors FindAllOf still returns, how many of the originals survive, and how far
# you have got. THE ANSWER IS THE DISTANCE AT WHICH THE COUNT FIRST DROPS. Standing still for
# two minutes is also a result — it is the control. R-1 is correct either way; this decides
# whether the release note says "fixed" or "hardened".
#15 pf_hook_building_actor_streaming
#
# ui-update-event — arms every Open/Show/Construct/Refresh/Update/Setup UFunction on the
# four UI classes, then wants: inventory open and closed, build menu opened, and a return
# to the TITLE SCREEN. Verdicts at 30 s, 90 s and 180 s. ⚠️ arms hooks UE4SS cannot remove.
#20 pf_hook_ui_update_event
#
# pal-spawned-fresh — the timestamps that say whether pal.spawned means a FRESH pal. Run it
# with a DELAY, because the delay is the measurement: `30 pf_hook_pal_spawned_fresh` makes
# world.ready exactly 30 s before t=0 and every distance below it exact. It prints a marker
# before each of: release a pal from the palbox, hatch/travel, run pf_spawn.
#30 pf_hook_pal_spawned_fresh
#
# skill-hit-source — confirms the NEGATIVE so the item can close: reads the damage structs
# out of the running build to show none carries a waza, then counts skill.hit against
# pal.damaged for two minutes while you FIGHT SOMETHING.
#20 pf_hook_skill_hit_source
#
# ui-host-layer — pushes a panel onto one of Palworld's own CommonUI layers through
# BP_AddWidget. Just LOOK for a teal panel top-left; it takes itself down at 45 s.
#20 pf_hook_ui_host_layer
#
# ui-backhandler — mounts a panel that CLAIMS the CommonUI back action, then asks you to
# PRESS ESC and report which of three things happened. Run ui-host-layer first: if that one
# cannot mount, this one cannot either and its silence would mean nothing.
#20 pf_hook_ui_backhandler
#
# building-runtime-reload — F-5, and the ONE thing you have to do is PRESS F9. The claim is
# that the building runtime survives a reload now, because the registry lives on _G and
# object_manager is on core/reload's KEEP list; nobody has ever pressed the key and looked.
# Each half runs on its own and this line is only the BEFORE half:
#     30 pf_hook_building_runtime_reload   (this line — records the baseline and re-arms)
#     press F9, then use a tracked structure
#     pf_hook building-runtime-reload      (the AFTER half, from the UE4SS console)
# It prints the identity of the registry table the SCAN closes over against the one the
# DISPATCH resolves against, the instance count on both sides of the press, and whether a
# building hook fired afterwards. ⚠️ It needs a REGISTERED Building definition to have
# anything to count — F-8 made the native catalogs declare without registering — so run
# require('palforge.native.buildings').publish('WorkBench') from the Lua console first, in a
# base with a workbench, and note that publishing is what starts persisting a record.
# It arms NO poller, on purpose: a live poller would refuse the very F9 it is asking for.
#30 pf_hook_building_runtime_reload
#
# ---- these WRITE, and each needs its own opt-in in palforge_dev.lua ------------------
#
# ⚠️ THE THREE STORE WRITERS BELOW STRETCH WHAT `writes` MEANS, ON PURPOSE. None of them
# touches Palworld's save. They create, corrupt and rewrite files under <Mods>/PalForge/state/
# — by the strict reading, the same class of change as mesh-texture-import's 82-byte PNG, which
# deliberately does NOT declare writes. They declare it anyway, because after the format-3 pass
# the claim being defended is that PalForge is careful with the files under its own directory,
# and a hook that made files in a player's state/ because somebody left env.debug on would be
# the first counter-example. Every one of them ends by printing the absolute path of what it
# left and the single call that removes it. Between them they use two pack ids and no more:
# `pf_probe` and `pf_crash`. No real pack may be called either.
#
#     env.debugHooks["store-save-roundtrip"] = true
# store-save-roundtrip — DOES A REAL SAVE'S STORE SURVIVE A REAL WORLD LOAD. The format-3 store
# has 553 headless checks behind it and has never once opened a file for a save Palworld
# actually loaded. Run it, and the short round trip completes inside the run: write through
# PalForge.pack(id).store, flush, drop the cache, read the bytes back off the real NTFS path,
# compare field by field, and check that <f>.json and <f>.json.bak both parse and differ.
# ⚠️ THEN QUIT TO THE TITLE SCREEN, LOAD THE SAME SAVE, AND RUN IT AGAIN — run 2 reads what run
# 1 wrote across a world teardown and a fresh load, and THAT is the trip a pack's onLoad makes.
# It deliberately does not clean up after itself, for exactly that reason.
#20 pf_hook_store_save_roundtrip
#
#     env.debugHooks["store-crash-recovery"] = true
# store-crash-recovery — WHICH COPY DOES THE LOADER READ WHEN A WRITE WAS INTERRUPTED. §7.3 of
# the design is a four-row table reasoned entirely from os.rename's semantics; the headless case
# file pins it on WSL2/ext4, and NTFS refuses an existing rename destination where POSIX
# replaces it. This plants a partial .tmp beside a good .json, a complete .tmp with no .json,
# and a lone .bak, and says which copy answered each time. It also plants an UNPARSEABLE file
# and checks that the loader refuses it, does not overwrite it, and moves it into _quarantine/
# byte for byte. ⚠️ THE QUARANTINE HALF NEEDS A FRESH SESSION — core/state reads a pack's file
# at most once per world, so run this one FIRST after a load or block [2] refuses by name.
#20 pf_hook_store_crash_recovery
#
#     env.debugHooks["save-survives-pack-removal"] = true
# save-survives-pack-removal — ⚠️ THE QUESTION THE WHOLE STORE PASS WAS STARTED FOR, and the
# only thing in this tree that can answer it. Removing PalForge is safe by construction (its
# state is a sidecar; the .sav is untouched). What is NOT settled is the other direction: a
# pack's `mypack_Potion` sits in the player's inventory in Palworld's own save, and that row
# exists only because PalSchema injected it. The load path is unreflected C++ (FPalBinaryMemory)
# — nothing to signature.describe, no UFunction to hook — so this is empirical or it is nothing.
# TWO LAUNCHES: run it in a THROWAWAY save while the pack is installed (the BEFORE snapshot),
# then QUIT, DELETE THAT PACK'S MOD FOLDER — both halves, the PalSchema JSON and the PalForge
# Lua — relaunch, load the same save, and run it again. Whether the save loads at all is the
# headline, and you will know it before the hook runs.
#20 pf_hook_save_survives_pack_removal
#
#     env.debugHooks["mesh-color-change"] = true
# mesh-color-change — hangs a chest in front of you and turns it red, green, blue. Nobody
# has ever watched a colour change; the parameter names are measured, the pixels are not.
#20 pf_hook_mesh_color_change
#
#     env.debugHooks["building-unlock"] = true
# building-unlock — ⚠️ NO UNDO: there is no "lock this again" call. Records every fact that
# can be established and then asks you to open the technology menu, because no accessor on
# this build can be asked instead.
#20 pf_hook_building_unlock
#
#     env.debugHooks["item-satiety-write"] = true
# item-satiety-write — CAN AN ITEM FEED ANYONE. Reads satiety and HP off your own character,
# then PRINTS THE PARAMETER LIST the live build declares for SetFullStomach — the one fact
# the C++ dump has no body for — and calls it ONLY if that walk succeeded and read a single
# scalar, restoring the exact value it found. Also fires AddHPByRate(-0.1), the one HP write
# on this build that carries no struct, and heals you again afterwards. A REFUSAL NAMING THE
# DECLARATION IS THE RESULT; nothing is pushed at an unread signature. Throwaway save.
#20 pf_hook_item_satiety_write
#
#     env.debugHooks["skill-projectile-spawn"] = true
# skill-projectile-spawn — WHY A PACK'S ACTIVE SKILL SPAWNS NOTHING. Prints what the running
# build declares for ShootOneBullet(Default), CreateChildSkillEffect, Initialize,
# BeginDeferredActorSpawnFromClass / FinishSpawningActor and FindWazaForBP, fires the ONE
# entry that takes no arguments (a real bullet — stand somewhere empty), and REFUSES every
# route that declares a struct, naming the parameter that stopped it. It settles
# core/spawn.lua's M.actor in the same block. Needs a pal near you; a pal with a RANGED
# weapon out is what makes block [2] measurable at all.
#20 pf_hook_skill_projectile_spawn
#
#     env.debugHooks["pal-skills-equip"] = true      (old spelling: _G.PALFORGE_TEST_WRITE_WAZA)
# pal-skills-equip — ⚠️ THE PUBLISH BLOCKER, AND THE ONE THAT ONCE PRECEDED THE GAME CLOSING
# BY 1.4 SECONDS. Writes an active move onto a live pal, exercises teachAll's partial-result
# contract and clearSkills, reads back after every write and puts everything back. USE A
# THROWAWAY SAVE, and have a pal standing next to you (pf_spawn, then wait ~10 s).
#pf_spawn
#40 pf_hook_pal_skills_equip
#
# audio-setvolume-audible — does not write and needs no opt-in, but it does move your
# character's output bus volume and put it back to 1.0. Plays one explosion at 1.0, 0.25,
# 0.0 and 1.0 and asks whether you heard the difference. TURN YOUR SPEAKERS UP.
#20 pf_hook_audio_setvolume_audible
